# Sensitive credentials — NEVER commit
api/config.php
Token.txt

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# SSL certificates (Hostinger auto-managed)
.well-known/

# Build output — bundled React (rebuild dari src/ setelah edit UI)
# Source React files di src/ (version control friendly)
