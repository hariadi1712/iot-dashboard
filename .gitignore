# Sensitive credentials — KEEP config.php template, tapi JANGAN push credentials nyata
# config.php di-commit dengan placeholder CHANGE_ME — edit manual setelah deploy
api/setup.php
Token.txt

# Node (if any)
node_modules/

# OS
.DS_Store
Thumbs.db

# SSL/certificates (Hostinger auto-managed)
.well-known/

# Build output (bundled React — rebuild after editing src/)
# Source React files are in src/ (development only)
